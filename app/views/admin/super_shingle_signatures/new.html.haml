.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", admin_super_shingle_signatures_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_admin_super_shingle_signature_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      SuperShingleSignature
    .inner
      = form_for :super_shingle_signature, :url => admin_super_shingle_signatures_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
