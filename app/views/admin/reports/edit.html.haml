.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", admin_reports_path
      %li= link_to "#{t("web-app-theme.new", :default => "New")}", new_admin_report_path
      %li.active= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_admin_report_path
  .content
    %h2.title
      = t("web-app-theme.edit", :default => "Edit")
      Report
    .inner
      = form_for @report, :url => admin_report_path(@report), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
