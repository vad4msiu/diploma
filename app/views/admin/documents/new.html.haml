.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", admin_documents_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_admin_document_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      Document
    .inner
      = form_for :document, :url => admin_documents_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
