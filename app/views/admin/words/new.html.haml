.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", admin_words_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_admin_word_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      Word
    .inner
      = form_for :word, :url => admin_words_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
