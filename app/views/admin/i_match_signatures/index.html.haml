.block
  .secondary-navigation
    %ul.wat-cf
      %li.first.active= link_to "#{t("web-app-theme.list", :default => "List")}", admin_i_match_signatures_path
      %li= link_to "#{t("web-app-theme.new", :default => "New")}", new_admin_i_match_signature_path
  .content
    %h2.title
      = t("web-app-theme.all", :default => "All")
      IMatchSignatures
    .inner
      %table.table
        %tr
          %th.first ID
          %th
            = t("activerecord.attributes.admin_i_match_signature.token", :default => t("activerecord.labels.token", :default => "Token"))
          %th= t("web-app-theme.created_at", :default => "Created at")
          %th.last &nbsp;
        - @i_match_signatures.each do |i_match_signature|
          %tr{:class => cycle("odd", "even")}
            %td
              = i_match_signature.id
            %td
              = link_to i_match_signature.token, admin_i_match_signature_path(i_match_signature)
            %td
              = i_match_signature.created_at
            %td.last
              = link_to "#{t("web-app-theme.show", :default => "Show")}", admin_i_match_signature_path(i_match_signature)
              |
              \#{link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_admin_i_match_signature_path(i_match_signature)} |
              \#{link_to "#{t("web-app-theme.delete", :default => "Delete")}", admin_i_match_signature_path(i_match_signature), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}"}
      .actions-bar.wat-cf
        .actions
        = will_paginate @i_match_signatures
- content_for :sidebar, render(:partial => 'sidebar')
